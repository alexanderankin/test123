The P4Plugin repository has moved.

The last revision available in this SVN repository is 17695, which
is the P4Plugin 0.4.0 stable release.

To get newer versions of the P4Plugin source code, use the git repository:

Anonymous read-only access:
git clone git://jedit.git.sourceforge.net/gitroot/jedit/P4Plugin

Developer access:
git clone ssh://[USER]@jedit.git.sourceforge.net/gitroot/jedit/P4Plugin

All previous releases and commits are available in the git repository.

