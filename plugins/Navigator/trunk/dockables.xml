<?xml version="1.0"?>

<!DOCTYPE DOCKABLES SYSTEM "dockables.dtd">
<!-- $Id$ -->
<DOCKABLES>
    <!-- TODO: need more logic here, if the user is using EditPane scope, then
    need to switch out the dockables when the editpane changes. May need to move
    this code into NavigatorPlugin. -->
	<DOCKABLE NAME="Navigator">
		ise.plugin.nav.NavigatorPlugin plugin = jEdit.getPlugin("ise.plugin.nav.NavigatorPlugin");
		return new ise.plugin.nav.Dockable(plugin.createNavigator(view));
	</DOCKABLE>
</DOCKABLES>
