<?xml version="1.0"?>

<!--

 This is the build.xml file for building the Minimap plugin.

 It is based on the standard build.xml file for jEdit plugins.

-->

<project name="Minimap" default="dist" basedir=".">

	<property file="build.properties"/>
	<property file="../build.properties"/>
	<property file="${user.home}/.build.properties"/>
	<property file="${user.home}/build.properties"/>
	<property name="dist.target" value="build" />
	<import file="${build.support}/plugin-build.xml" />
    <property name="docs-proc.target"  value="none"/>

    <selector id="packageFiles">
    	<filename name="description.html" />
    </selector>
</project>

<!-- :indentSize=4:lineSeparator=\n:noTabs=false:tabSize=4: -->
