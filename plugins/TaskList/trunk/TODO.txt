TODO:

version 0.4.2
=============

- docs to reST?
- put tasklist into it's own package
- delete task and delete tag into Task class?
- explicit re-parse in TaskList popup?
- fix "Reset task patterns"
- fix 699048
- integrate with SideKick's work request API

future
======

- buffer sets: display tasks for current buffer, buffers 
  visible in view, all buffers, others?

	// interface something like this?
	interface BufferSet{
		Buffer[] getBuffersInSet();
		int getBufferCount();
	}

- multi-line task comments
- parse text in text mode?

:lineSeparator=\n:maxLineLen=72:noTabs=false:tabSize=4:wrap=hard:
