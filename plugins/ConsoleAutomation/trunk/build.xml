<?xml version="1.0"?>

<project name="ConsoleAutomation" default="dist" basedir=".">

	<property file="build.properties"/>
	<property file="../build.properties"/>
	<property file="${user.home}/.build.properties"/>
	<property file="${user.home}/build.properties"/>
	<property name="dist.target" value="build" />
	<import file="${build.support}/plugin-build.xml" />

    <selector id="packageFiles">
    	<filename name="description.html" />
    </selector>

	<path id="project.class.path">
		<pathelement location="${jedit.install.dir}/jedit.jar"/>
		<pathelement location="commons-net-2.0.jar"/>
		<pathelement location="${src.dir}"/>
	</path>

	<target name="build.prepare" >
	 	<mkdir dir="${build.dir}" />
		<copy file="commons-net-2.0.jar" todir="${install.dir}" />
	</target>

</project>
