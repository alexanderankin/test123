<?xml version="1.0"?>
<project name="CppCheck" default="build" basedir=".">

	<property file="build.properties"/>
	<property file="../build.properties"/>

	<property file="build.support" value="../build-support" />
	<import file="${build.support}/plugin-build.xml" />

	<property name="jedit.plugins.dir" value="${install.dir}" />

	<selector id="packageFiles">
	  <or>
		<filename name="docs/index.txt" />
	  </or>
	 </selector>

	<property name="jar.name"  value="${ant.project.name}.jar"/>

	<property name="src.dir" location="${basedir}/src"/>
	<property name="build.dir" value="build"/>

	<path id="project.class.path">
		<pathelement location="${jedit.install.dir}/jedit.jar"/>
		<pathelement location="${jedit.plugins.dir}/ErrorList.jar"/>
		<pathelement location="${jedit.plugins.dir}/ProjectViewer.jar"/>
	</path>

	<target name="build.prepare" >
	 	<mkdir dir="${build.dir}" />
	</target>

</project>

