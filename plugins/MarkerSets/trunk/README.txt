The MarkerSets source repository has moved.

To get newer versions of the plugin source code, use the git repository:

Anonymous read-only access:
git clone git://jedit.git.sourceforge.net/gitroot/jedit/MarkerSets

Developer access:
git clone ssh://[USER]@jedit.git.sourceforge.net/gitroot/jedit/MarkerSets


All previous releases and commits remain in SVN.