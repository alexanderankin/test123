XML source repository has moved (2019-12-09).

To get newer versions of the plugin source code, use the git repository:

Anonymous read-only access:
git clone git://git.code.sf.net/p/jedit/XML jedit-XML

Developer access:
git clone ssh://kerik-sf@git.code.sf.net/p/jedit/XML jedit-XML

Git-Browse:
https://sourceforge.net/p/jedit/XML/ci/master/tree/

All previous releases and commits remain in SVN.
