- HTML: taglist.xml
- Tag palette

- tag completion:
  - option to set delay before showing popup
  - <: shows popup
  - </: in XML mode, auto close last tag; in HTML mode, tag list
- root document property, proper handling of tags in nested files
- public id option pane

- investigate typing lag
- handle parsing open buffers better
- OASIS catalog support
- move some HTML plugin features (tag commands, tag highlight) to XML
  plugin
